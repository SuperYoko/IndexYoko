version: '3.8'

services:
  homepage-canvas:
    build: .
    container_name: homepage-canvas
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
